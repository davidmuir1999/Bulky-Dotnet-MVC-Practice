{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "__comment__1": "Below is custom key for setting up SQL",
  "ConnectionStrings":{
    "DefaultConnection": "Server: DESKTOP-V7DUP9T;Database=Bulky;Trusted_Connection=True;TrustServerCertificate=True"
  },
  "__comment__2": "Above we are defining  a key value pair, were we have to define the value. DefaultConnection defines the server, which we set up in SSMS. The next property to define is the database name. The next two properties are needed when authenticating to SQL server, it sometimes needs a trust certificate"
}

